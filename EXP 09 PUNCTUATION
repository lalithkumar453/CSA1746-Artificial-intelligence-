import string

# Given string
input_string = "Hello!!!, Python: is fun; isn't it?"

# Remove punctuations
no_punct = input_string.translate(str.maketrans("", "", string.punctuation))

# Display result
print("Original String:", input_string)
print("String without Punctuations:", no_punct)
